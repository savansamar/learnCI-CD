name: GitHub Actions Demo
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀

on: [push, pull_request]

jobs:
  my_first_job:
    name: My first job
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder Step
        run: echo "This job has no steps defined."

  my_second_job:
    name: My second job
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder Step
        run: echo "This job also has no steps defined."