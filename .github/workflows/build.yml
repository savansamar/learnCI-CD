name: GitHub Actions Demo
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀

on: [push, pull_request]

jobs:
  my_first_job:
    name: My first job
  my_second_job:
    name: My second job